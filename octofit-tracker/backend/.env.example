# The OctoFit API listens on this port locally and in Codespaces.
PORT=8000

# Use the local MongoDB instance for the octofit database.
MONGODB_URI=mongodb://localhost:27017/octofit_db

# Codespaces sets this automatically. Leave it unset for localhost development.
# CODESPACE_NAME=your-codespace-name